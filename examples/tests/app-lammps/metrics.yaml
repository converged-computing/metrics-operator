apiVersion: flux-framework.org/v1alpha1
kind: MetricSet
metadata:
  labels:
    app.kubernetes.io/name: metricset
    app.kubernetes.io/instance: metricset-sample
  name: metricset-sample
spec:
  # Number of indexed jobs to run netmark on
  pods: 2
  metrics:
   # This uses the default commands
   - name: app-lammps

     # This should the default - you are responsible for asking for the right number of processes,
     # lammps arguments, and calling mpirun to point at an expected hostfile in the workdir.
     # options:
     #  command: mpirun --hostfile ./hostlist.txt -np 2 --map-by socket lmp -v x 2 -v y 2 -v z 2 -in in.reaxc.hns -nocite